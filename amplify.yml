version: 1
frontend:
  phases:
    build:
      commands:
        - "npm install"
        - "npm run build"
  artifacts:
    baseDirectory: build/client
    files:
      - "**/*"
  cache:
    paths: []