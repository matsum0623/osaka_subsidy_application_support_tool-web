version: 1
frontend:
  phases:
    build:
      commands:
        - echo $REST_API
        - npm install
        - npm run build
  artifacts:
    baseDirectory: build/client
    files:
      - "**/*"
  cache:
    paths: []