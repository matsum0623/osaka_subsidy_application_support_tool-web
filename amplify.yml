version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - npm run build:$BUILD_ENV
  artifacts:
    baseDirectory: build/client
    files:
      - "**/*"
  cache:
    paths: []